#### Catalog

- [1. Overview of wrapper classes](#1__2)
- [2. Packing and unpacking, basic type and string conversions](#2__22)
- [3. Integer buffer](#3_Integer_114)

## 1. Overview of wrapper classes

- Reference data types corresponding to basic data types
- Object unifies all data, the default value of the wrapper class is null

  | Basic data types | Wrapper types |
  | --- | --- | byte | byte | byte | byte | byte | byte | byte
  | byte | Byte |
  | short | Short |
  | int | Integer |
  | long | Long |
  | long | Long | float | Float |
  | double | Double | boolean | Boolean | boolean | boolean | boolean
  | boolean | Boolean | char | Character | Character | Character | Character | Character | Character
  | char | Character |

## 2. Packing and unpacking, basic type and string conversions

- 8 wrapper classes provide ways to convert between different types:

  - 6 common methods provided in the Number parent class

    ```java
    public class Test {
        public static void main(String[] args) {
            // 基本类型数据
            int num1 = 10;

            // JDK1.5之前
            // 类型转换：装箱，基本类型转成引用类型的过程
            // 使用Integer类创建对象
            Integer integer1 = new Integer(num1);
            Integer integer2 = Integer.valueOf(num1);

            System.out.println(integer1);
            System.out.println(integer2);

            // 类型转换：拆箱，引用类型转成基本类型
            Integer integer3 = new Integer(20);
            int num2 = integer3.intValue();

            System.out.println(integer3);
            System.out.println(num2);

            // JDK1.5之后
            int num3 = 30;
            // 自动装箱
            Integer integer4 = num3;

            System.out.println(integer4);
            // 自动拆箱
            int num4 = integer4;

            System.out.println(num4);
        }
    }
    ```
  - parseXXX() static method

    ```java
    public class Test {
        public static void main(String[] args) {
            // 基本类型和字符串之间转换
            // 基本类型转成字符串
            int num1 = 10;

            // 使用+号
            String string1 = num1 + "";

            System.out.println(string1);

            // 使用Integer中的toString()方法
            String string2 = Integer.toString(num1);

            System.out.println(string2);

            String string3 = Integer.toString(num1, 2);

            System.out.println(string3);

            // 字符串转成基本类型
            String string4 = "20";

            // 使用Integer中的parseXXX()方法
            int num2 = Integer.parseInt(string4);

            System.out.println(num2);

            // boolean字符串形式转成基本类型，"true" -> true，非"true" -> false
            String string5 = "true";
            boolean boolean1 = Boolean.parseBoolean(string5);
            System.out.println(boolean1);

            String string6 = "";
            boolean boolean2 = Boolean.parseBoolean(string6);
            System.out.println(boolean2);

        }
    }
    ```
  - valueOf() static method.
- Note: Type compatibility must be ensured, otherwise a Number Format Exception will be thrown.

## 3. Integer buffer

- Java pre-created 256 commonly used integer wrapper type objects (-128~127)
- In practice, the reuse of the created object

```java
public class Test {
    public static void main(String[] args) {
        Integer integer1 = new Integer(100);
        Integer integer2 = new Integer(100);
        System.out.println(integer1 == integer2);

        Integer integer3 = 100;
        Integer integer4 = 100;
        System.out.println(integer3 == integer4);

        Integer integer5 = 200;
        Integer integer6 = 200;
        System.out.println(integer5 == integer6);
    }
}
```