# Catalog

- [1. Classification of Iris](#1__2)
- [1.1 Getting to know the data for the first time](#11__11)
  - [1.2 Measuring model success: training vs. test data](#12__30)
  - [1.3 First things first: looking at the data](#13__54)
  - [1.4 Build the first model: kNeighborsClassifier](#14_kKNeighborsClassifier_90)
  - [1.5 Make predictions (predict)](#15_predict_105)
  - [1.6 Evaluate the model](#16__126)

# 1. iris classification

- Predict one of multiple options (species of iris): **Categorization** problem
- Possible outputs (different varieties of iris): **Category**
- Expected output for a single data point (an iris): variety of this flower
- For a single data point, its variety: **Label**

## 1.1 Getting to know the data for the first time

- Using the Iris dataset

```python
from sklearn.datasets import load_iris

dataset = load_iris()

print(dataset.keys())
# dict_keys(['data', 'target', 'frame', 'target_names', 'DESCR', 'feature_names', 'filename', 'data_module'])
# data: 测量数据（150*4）
# target: 每朵花的品种（150）
# target_names: 花的品种（3）
# feature_names: 对特征进行说明（4）
```

## 1.2 Measuring model success: training vs. test data

- Data used to build the model cannot be used to evaluate the model

  - Cannot tell us how well the model **generalizes** (can it predict correctly on new data)
- Split the collected data into two parts

  - One part is used to build the machine learning model: **training data** or **training set**.
  - The other part is used to evaluate the performance of the model: **test data**, **test set** or **leave out set**

```python
from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split

dataset = load_iris()
X_train, X_test, y_train, y_test = train_test_split(dataset["data"], dataset["target"], random_state=0)
# X: 数据
# y: 标签
# random_state: 随机数生成器的种子
# 训练数据占比75%
# 测试数据占比25%
```

## 1.3 Important thing #1: Observe the data

- Methods of observing data: visualization
  - Plotting a **scatter plot**
    - A feature as
      x
      x
      x-axis
    - One feature as
      y
      y
      y-axis
    - Disadvantages: can only plot two or three features, difficult to plot datasets with more than three features
  - Plotting **Scatter Plot Matrix**
    - View all features in pairs
    - Disadvantage: can't show the relationship between all features at the same time

```python
from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split
import pandas as pd
import matplotlib.pyplot as plt

dataset = load_iris()
X_train, X_test, y_train, y_test = train_test_split(dataset["data"], dataset["target"], random_state=0)
dataframe = pd.DataFrame(X_train, columns=dataset["feature_names"])
pd.plotting.scatter_matrix(frame=dataframe, c=y_train, figsize=(15, 15), marker="o", hist_kwds={"bins": 20}, s=60, alpha=0.8, cmap=plt.cm.get_cmap('Pastel1'))
# c: 颜色
# figsize: 图的尺寸
# marker: 点的样式
# hist_kwds: 柱状图的样式
# s: 点的半径
# alpha: 透明度
# cmap: 配色风格
plt.show()
```

! [Scatter matrix of the Iris dataset, colored by category labels](https://i-blog.csdnimg.cn/blog_migrate/d00c29aacdcd8baf15ea6cbf7d75e732.png#pic_center)

## 1.4 Building the first model: kNeighborsClassifier

```python
from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split
from sklearn.neighbors import KNeighborsClassifier

dataset = load_iris()
X_train, X_test, y_train, y_test = train_test_split(dataset["data"], dataset["target"], random_state=0)
knn = KNeighborsClassifier(n_neighbors=1)
knn.fit(X_train, y_train)
```

## 1.5 Make a prediction (predict)

```python
from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split
from sklearn.neighbors import KNeighborsClassifier
import numpy as np

dataset = load_iris()
X_train, X_test, y_train, y_test = train_test_split(dataset["data"], dataset["target"], random_state=0)
knn = KNeighborsClassifier(n_neighbors=1)
knn.fit(X_train, y_train)
X_new = np.array([[5, 2.9, 1, 0.2]])
# 与训练集格式对应
prediction = knn.predict(X_new)
print(f"{prediction}: {dataset['target_names'][prediction]}")
# [0]: ['setosa']
```

## 1.6 Evaluate the model

- Calculate **Accuracy** to measure the model's strengths and weaknesses
  - Precision: percentage of varieties predicted correctly

```python
from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split
from sklearn.neighbors import KNeighborsClassifier
import numpy as np

dataset = load_iris()
X_train, X_test, y_train, y_test = train_test_split(dataset["data"], dataset["target"], random_state=0)
knn = KNeighborsClassifier(n_neighbors=1)
knn.fit(X_train, y_train)
y_pred = knn.predict(X_test)
print(f"score(np.mean) : {np.mean(y_test == y_pred)}")
# score(np.mean) : 0.9736842105263158
# 参数: 布尔表达式（测试标记与预测标记是否相等）
print(f"score(knn.score) : {knn.score(X_test, y_test)}")
# score(knn.score) : 0.9736842105263158
# 参数: 测试数据，测试标记
```